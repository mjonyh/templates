%% Chapter 3: Results (chapters/chapter03.tex)

% ==============================================================================
% CONTENT BLUEPRINT — Chapter 3: Results
% ==============================================================================
% Reading audience: examiner who will check whether the data support the
% claims in Chapter 1.
% Goal: present all measured / computed results clearly, objectively, and
% without interpretation. Interpretation belongs to Chapter 4.
%
% Recommended length: 10–15 pages. Order results in the same logical order
% as the objectives in §1.4 — this makes it obvious whether each objective
% was met.
%
% ----------------------------------------------------------------------------
% §3.1 Experimental / Computational Dataset  (1–2 pages)
%   • State the size of the dataset (how many samples, runs, sweeps,
%     configurations).
%   • Compactly tabulate the categorical parameters used (laser wavelength,
%     input intensity, sample thickness, etc.) — typically one summary
%     table at the start of the chapter.
%   • All numerical values MUST be rendered with siunitx (\num{...} and
%     \si{...}); avoid raw decimals in prose. 0.85e-16 → \num{0.85e-16}.
%
% §3.2 Primary Results  (4–6 pages, the bulk of the chapter)
%   • Order figures/tables by objective. One figure per finding.
%   • Each figure/table gets a self-contained caption. Captions MUST contain:
%       – What is plotted / tabulated,
%       – At what conditions (T, λ, intensity, etc.),
%       – What the trend or magnitude is in words, so the reader does not
%         need to read the prose to interpret the image.
%   • Every figure must be referenced in the prose before and after the
%     display (introduce before, summarise the trend after).
%   • State uncertainty on every reported value, with units.
%
% §3.3 Secondary Results and Auxiliary Checks  (1–3 pages)
%   • Sanity checks: agreement with literature baselines, sensitivity to
%     parameters held fixed, repeatability runs.
%   • Each check answers one specific concern from §1.3 (problem statement)
%     or §1.5 (scope).
%
% §3.4 Limitations Observed in the Data  (½–1 page)
%   • Note where the data fall short of objective — incomplete sweeps,
%     noise floor, systematic drift, etc.
%   • This MUST be honest. Suppressing limitations here and acknowledging
%     them only in Chapter 4 is a methodological red flag.
%
% Writing-style rules for this chapter:
%   • Past tense. This is what was found.
%   • No causal language ("because", "due to" interpretive claims).
%     Restrict verbs to: shows, displays, exhibits, yields, measures,
%     computes. Save "explains / causes / proves" for Chapter 4.
%   • No "we can see that X" — the figure caption already states X.
%   • Provide raw data alongside this document (Zenodo DOI / GitHub tag)
%     and link to it.
% ==============================================================================

\chapter{Results}
\label{ch:results}

This chapter details the compiled results from Z-scan experiments and validates the simulated curves.

\section{Experimental Dataset}
Table \ref{tab:material_properties} details the linear and non-linear optical properties recorded for typical calibration samples at a wavelength of $\lambda = \SI{632.8}{\nano\meter}$.

\begin{table}[htbp]
    \centering
    \caption{Linear and non-linear optical parameters of measured samples.}
    \label{tab:material_properties}
    \begin{tabular}{ccc}
        \toprule
        \textbf{Sample Name} & \textbf{Linear Index ($n_0$)} & \textbf{Non-linear Index ($n_2$)} \\
         & & ($\times \num{1e-16}\,\SI{}{\meter\squared\per\watt}$) \\
        \midrule
        Quartz Glass & \num{1.457} & \num{0.85} \\
        Fused Silica & \num{1.458} & \num{0.78} \\
        SF59 Glass   & \num{1.943} & \num{57.0} \\
        \bottomrule
    \end{tabular}
\end{table}

\section{Transmission Curves (pgfplots)}
Using the raw data model, we generate the normalized closed-aperture transmission profile using the \textsf{pgfplots} environment, as illustrated in Figure \ref{fig:transmission_plot}:

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.8\textwidth}
        \centering
        \begin{tikzpicture}
        \begin{axis}[
            width=\textwidth,
            height=6cm,
            xlabel={Normalized Distance ($z/z_0$)},
            ylabel={Normalized Transmission $T(z)$},
            xmin=-4, xmax=4,
            ymin=0.8, ymax=1.2,
            grid=both,
            grid style={line width=.1pt, color=gray!10},
            major grid style={line width=.2pt, color=gray!30},
            legend pos=north west
        ]
            % Simulated transmission curve
            \addplot[blue, thick, domain=-4:4, samples=100]
                {1 + (4*0.3*x)/((x^2 + 9)*(x^2 + 1))};
            \addlegendentry{Simulation ($\Delta \Phi_0 = 0.3$)}

            % Mock experimental data points
            \addplot[red, only marks, mark=o] coordinates {
                (-3.5, 0.96) (-3.0, 0.94) (-2.5, 0.92) (-2.0, 0.89)
                (-1.5, 0.87) (-1.0, 0.88) (-0.5, 0.92) (0.0, 1.00)
                (0.5, 1.08) (1.0, 1.12) (1.5, 1.13) (2.0, 1.11)
                (2.5, 1.08) (3.0, 1.06) (3.5, 1.04)
            };
            \addlegendentry{Experimental}
        \end{axis}
        \end{tikzpicture}
    \end{subfigure}
    \caption{Closed-aperture Z-scan normalized transmission curve showing peak-valley behavior.}
    \label{fig:transmission_plot}
\end{figure}
