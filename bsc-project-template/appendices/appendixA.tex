%% Appendix A: Supplementary Derivations (appendices/appendixA.tex)

% ==============================================================================
% CONTENT BLUEPRINT — Appendix A: Supplementary Material
% ==============================================================================
% Appendices are reference material. The reader should be able to skip the
% main text and read the appendix on its own.
%
% Recommended content by appendix type:
%
%   Mathematical Derivations
%     • Move any derivation that interrupts the flow of Chapter 2 here.
%     • Each derivation should be self-contained: state assumptions, write
%       every intermediate step (this is appendix, not the main text — no
%       "it can be shown that" shortcuts).
%     • End each derivation with a "Result" boxed equation that the reader
%       can quote directly. Restate where it is used in the main text.
%     • Use Appendix subsection numbering: \section{}, \subsection{}, etc.
%
%   Tables of Calibration Constants
%     • Tabulate any constants used repeatedly across chapters.
%     • Include source of constant, uncertainty, reference, date obtained.
%     • Alias: introduce the constant once in the main text (\nomenclature),
%       then list values here.
%
%   Code Listings
%     • Full source of any analysis script not already in §2.5.
%     • Include comments inside listings explaining non-obvious lines.
%     • Cross-reference from any chapter that uses the script's output.
%
%   Datasheets / Equipment Sheets
%     • Photocopy or retypeset key instrument spec tables.
%     • State serial number, calibration date, certificate reference.
%
%   Raw Data Tables
%     • Any dataset too large for the main chapter but referenced there.
%     • Provide a short caption explaining what each column means.
%
% Writing-style rules for appendices:
%   • No narrative voice. Pure reference. Sentences are explanations of
%     derivations or descriptions of tables.
%   • Equations are numbered within the appendix (A.1, A.2, ...) — but
%     the appendix counter requires Appendix prefix in display. If your
%     report uses Eq. labels like eq:*, just label them B.results.
%   • Each appendix ends with one or two cross-references back into the
%     main text (where this appendix is invoked).
% ==============================================================================

\appendix
\chapter{Supplementary Calculations}
\label{app:calculations}

This appendix presents auxiliary calculations supporting the simulations.

\section{Gaussian Beam Radius Derivation}
The radius $w(z)$ of a Gaussian laser beam propagating along the $z$-axis is given by:
\begin{equation}
    w(z) = w_0 \sqrt{1 + \left(\frac{z}{z_0}\right)^2}
    \label{eq:beam_radius}
\end{equation}
where $w_0$ is the beam waist radius and $z_0 = \frac{\pi w_0^2}{\lambda}$ is the Rayleigh range.

The peak intensity $I(z)$ at a given distance $z$ is:
\begin{equation}
    I(z) = I_0 \left( \frac{w_0}{w(z)} \right)^2 = \frac{I_0}{1 + \left(\frac{z}{z_0}\right)^2}
    \label{eq:intensity_distribution}
\end{equation}
where $I_0$ represents peak intensity at the focus ($z=0$). These equations are used to model the transmission coordinates plotted in Chapter \ref{ch:results}.
